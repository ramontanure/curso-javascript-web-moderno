const raio = 5;
const area = Math.PI * Math.pow(raio, 2);
//Math é um obejto que tem uma constante que calcula PI e a pontencia
//Assim sendo raio elevado a 2 * PI
console.log(area);
console.log(typeof Math);
