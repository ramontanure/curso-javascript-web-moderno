const teste = ["Ramon", "Rafael", "Leonardo", "Camila"];

function foreach2(callback) {
  for (let i = 0; i < this.length; i++) {
    callback(p[teste2], p, this);
  }

  //Outro que da certo
  let p = 0;
  while (p < teste2.length) {
    console.log(`${p} ${p[teste2]}`);
    p++;
  }
}
